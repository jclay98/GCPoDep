<% if logged_in? && current_user.role?(:commish) %>
  <%= render partial: "partials/structure1", locals: {model_name: "officer", 
                                                         primary: "tabbed_index", 
                                                         secondary: nil,
                                                         sidebar: "officers_sidebar"} %>
<% elsif logged_in? && current_user.role?(:chief) %>
  <%= render partial: "partials/unit_officers", locals: {object: @unit} %>
  <%= render partial: "partials/structure1", locals: {model_name: "officer", 
                                                         primary: nil, 
                                                         secondary: "tabbed_index",
                                                         sidebar: "officers_sidebar"} %>
<% else  %>
  <p>Unauthorized to view Officers</p>
<% end %>

